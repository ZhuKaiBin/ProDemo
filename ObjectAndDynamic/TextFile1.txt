在.NET中，object和dynamic也有一些区别：

1、object：object是.NET中的顶级类，所有类都是object的子类。在C#中，你可以使用object关键字来声明变量，该变量可以存储任何类型的对象引用。

2、dynamic：dynamic是.NET中的一种类型，它允许你在运行时执行动态绑定。这意味着你可以在编译时避免类型检查，而是在运行时确定变量的类型。这使得代码更加灵活，但也可能导致运行时错误。